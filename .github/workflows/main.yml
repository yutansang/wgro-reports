name: Ultra Parallel Generation (3 Lanes)

permissions:
  contents: write

on:
  schedule:
    - cron: '0 * * * *' # 每小时运行
  workflow_dispatch:    # 允许手动触发

jobs:
  # ==================================================
  # 🚗 车道一：快车道 (zg, mg, wh)
  # ==================================================
  fast-lane:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.14'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install yfinance pandas numpy
          # 快脚本不需要 baostock

      - name: Run Fast Scripts 🚀
        run: |
          echo ">>> [快车道] 开始运行快脚本..."
          python zg.py
          python mg.py
          python wh.py
          echo ">>> [快车道] 完成！"

      - name: Deploy Fast Reports
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: .
          clean: false # 关键：不删别人文件
          git-config-email: action@github.com
          git-config-name: GitHub Action

  # ==================================================
  # 🐢 车道二：慢车道 A (agd.py)
  # ==================================================
  slow-lane-agd:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.14'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          # 慢脚本需要 baostock
          pip install yfinance pandas numpy baostock

      - name: Run AGD Script 🐢
        run: |
          echo ">>> [慢车道A] 开始运行 agd.py..."
          python agd.py
          echo ">>> [慢车道A] 完成！"

      - name: Deploy AGD Report
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: .
          clean: false # 关键
          git-config-email: action@github.com
          git-config-name: GitHub Action

  # ==================================================
  # 🐢 车道三：慢车道 B (agx.py)
  # ==================================================
  slow-lane-agx:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.14'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install yfinance pandas numpy baostock

      - name: Run AGX Script 🐢
        run: |
          echo ">>> [慢车道B] 开始运行 agx.py..."
          python agx.py
          echo ">>> [慢车道B] 完成！"

      - name: Deploy AGX Report
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: .
          clean: false # 关键
          git-config-email: action@github.com
          git-config-name: GitHub Action
